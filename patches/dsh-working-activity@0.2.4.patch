diff --git a/lib/types/client/index.d.ts b/lib/types/client/index.d.ts
index 40c47bc5facbbab3830e353758b732713484460d..fddf6963528dc9cfe551bc2e0dfcd02fa49b9ade 100644
--- a/lib/types/client/index.d.ts
+++ b/lib/types/client/index.d.ts
@@ -6,7 +6,7 @@
  * the live frame through the standard session kit and owns no store, no
  * refresh chain, and no event listener.
  *
- * Mount contract (see the root README's "Web UI 集成" section): the web
+ * Mount contract (see the root README's "Web UI integration" section): the web
  * client's client-modules host scans loader entries for `dsh.client`
  * declarations and serves this package's `./client` bundle at
  * /plugins/dsh-working-activity/client.js. The entry contributes into the
diff --git a/lib/types/index.js b/lib/types/index.js
index 9aad4ac0a3857693163018f170f5a55dc1f33f4c..0cd9ced5e006da909192822d239445d2c1c109f5 100644
--- a/lib/types/index.js
+++ b/lib/types/index.js
@@ -32,7 +32,7 @@ export const Config = z.object({
     narrate: z.boolean().default(true),
 });
 /** The self-narration contract injected into the system prompt (narrate on). */
-const NARRATE_INSTRUCTION = '[状态栏] 你有一个状态栏展示给用户。【必须】在每个步骤/子任务开始时（不只是调用工具前），在回复正文的最前面单独写一行：⏵ 你在做的具体事情（不超过20字），然后换行继续正常回复。整轮回复只写一行 ⏵，不要重复。信息为主——让人一眼知道你在干什么，风格自然、可以带点俏皮。例：⏵ 修复登录页样式、⏵ 查一下报错原因、⏵ 给补丁跑个验证。切换任务时必须更新。';
+const NARRATE_INSTRUCTION = '[Status bar] You have a status bar shown to the user. You MUST start each step/subtask (not only before tool calls) with a single line at the very top of your reply: ⏵ a short concrete description of what you are doing (at most 20 words), then a newline and your normal reply. Write only one ⏵ line per turn. Keep it informative so the user can see what you are doing. Examples: ⏵ Fix login page styles, ⏵ Check the error cause, ⏵ Run the patch tests. Update the line when you switch tasks. Always write the ⏵ line in English.';
 /**
  * Wire the working-activity plugin.
  * @param ctx - Cordis context (agent loop + session services composed).
diff --git a/lib/types/phrases.js b/lib/types/phrases.js
index 0bb3fc893925b74a4ac25ab941e6cd92442b5380..e0969dc30c40ed3a9aaf65a84855d769a54e3381 100644
--- a/lib/types/phrases.js
+++ b/lib/types/phrases.js
@@ -1,107 +1,118 @@
 /**
  * Copy pools for the working-activity status line: short, colloquial, playful
- * Chinese fragments with deadpan English one-liners mixed in, matching the
- * pi-working-activity tone. Everything here is pure data + pure pickers.
+ * English fragments, matching the pi-working-activity tone. Everything here
+ * is pure data + pure pickers.
  * @module @deepseek-ai/dsh-working-activity/phrases
  */
+
+/** A pool of copy fragments. */
 /** Pick one random entry; repeated draws avoid the previous entry when possible. */
 export function pickPhrase(entries, previous) {
-    if (entries.length === 0)
-        throw new Error('pickPhrase() requires a non-empty pool');
-    if (entries.length === 1)
-        return entries[0];
-    let next = entries[Math.floor(Math.random() * entries.length)];
-    let guard = 0;
-    while (next === previous && guard++ < 8) {
-        next = entries[Math.floor(Math.random() * entries.length)];
-    }
-    return next;
+  if (entries.length === 0) throw new Error('pickPhrase() requires a non-empty pool')
+  if (entries.length === 1) return entries[0]
+  let next = entries[Math.floor(Math.random() * entries.length)]
+  let guard = 0
+  while (next === previous && guard++ < 8) {
+    next = entries[Math.floor(Math.random() * entries.length)]
+  }
+  return next
 }
+
 /** Thinking phrases while the model works without a tool. */
 export const THINKING_PHRASES = [
-    '嗯…让我捋捋', '盘一下盘一下', '大脑转起来了', '思考.gif', '给我一秒', '脑子在冒烟',
-    '想呢想呢', '别催别催', '啾，让我想想', '让我琢磨下', '嗯…等一下哦', '正在盘逻辑',
-    '小脑瓜动一下', '嗯？哦…', '让我理理', '翻翻脑子', '回想中', '等一下下', '让我嗅嗅',
-    '脑内风暴中', '嗯…让我品品', '滴滴滴思考中', '稍等，在想', '盘明白了么', '挠头…',
-    '让子弹飞一会', '让我脑补一下', '加载中', '你说 我在听', '噢…是这样', '让我嚼一嚼',
-    '嗯…有点意思', '搓搓手想想', '等下，在想', '让我康康', '想好了告诉你', '脑子转圈圈',
-    '嗯…让我反应下', '等下下嘛', '思路加载中', '琢磨中', '嗯…让我拆一下', '盘，都可以盘',
-    '让我嗅探一下', '脑内跑火车', '嗯…让我缓一下', '滴滴，想呢', '思索.jpg', '嗯…有点东西',
-    '让我品', '小跑一下思路', '等下，有画面了', '让我咀嚼', '嗯…发会儿呆', '思考泡泡',
-    '脑电波传输中', '嗯…转转', '等下，盘好了', '让我回味', '滴滴滴', '思考的鱼',
-    '嗯…让我摸一下', '脑子在煮咖啡', '等下，我打个腹稿', '嗯…重启一下', '让我挠墙',
-    '嗯，来了来了', '脑子冒泡泡', '嗯…有点烫', '思考猫猫', '让我咕噜一下', '嗯…盘它',
-    '等下，我闪个思路', '脑子在蹦迪', '嗯…', '让我想想', '盘一下', '啾', 'lol', 'hm', 'oh',
-    'ok', 'um', 'heh', 'uh', 'nah', 'mm', 'wow', 'nice', 'rgrg', 'okk', 'hhh', 'emm', 'emmm',
-    'CPU烧了', '让我打个log看看', '先跑一下试试', '定位一下', '排查一下', '看看日志',
-    'loading 99%', '让我捋一下逻辑',
-];
+  'hmm, let me think', 'working it out', 'brain spinning up', 'thinking.gif', 'one sec', 'wheels turning',
+  'thinking…', 'hold on', 'let me see', 'mulling it over', 'hang on a sec', 'sorting the logic',
+  'gears turning', 'hmm? oh…', 'lining it up', 'poking around', 'recalling', 'just a moment', 'sniffing this out',
+  'storm in progress', 'hmm, tasting that', 'thinking… beep', 'wait, thinking', 'figured it yet?', 'scratching my head…',
+  'let it cook', 'filling in the picture', 'loading', 'I hear you', 'oh… got it', 'chewing on it',
+  'hmm, interesting', 'rubbing hands', 'wait, thinking', 'let me look', 'I will tell you when I have it', 'spinning',
+  'hmm, processing', 'one more sec', 'loading the idea', 'mulling', 'hmm, breaking it down', 'let me work this',
+  'sniffing around', 'train of thought', 'hmm, give me a beat', 'beep, thinking', 'ponder.jpg', 'hmm, something here',
+  'let me taste that', 'jogging the thought', 'wait, picture forming', 'chewing', 'hmm, zoning out', 'think bubbles',
+  'brainwaves incoming', 'hmm, spinning', 'wait, almost there', 'savoring it', 'beep beep', 'thinking fish',
+  'hmm, poking it', 'brain on coffee', 'wait, drafting', 'hmm, reboot', 'climbing the wall',
+  'ok, coming', 'brain bubbling', 'hmm, a bit hot', 'thinking cat', 'let me rumble', 'hmm, work it',
+  'wait, a spark', 'brain on disco', 'hmm…', 'let me think', 'work it', 'hey', 'lol', 'hm', 'oh',
+  'ok', 'um', 'heh', 'uh', 'nah', 'mm', 'wow', 'nice', 'rgrg', 'okk', 'hhh', 'emm', 'emmm',
+  'CPU smoking', 'let me check a log', 'try a run first', 'pin it down', 'dig into it', 'check the logs',
+  'loading 99%', 'let me straighten the logic',
+]
+
 /** Tiered phrases when thinking runs long (elapsed >= threshold). */
 export const THINKING_TIERS = [
-    { atMs: 30_000, pool: ['嗯，让我细想想', '30秒了，还在盘', '等下，快好了', '别急，就快出结果了', '让我再捋一捋', '嗯…思路没断', '30秒，快了', '等等，有眉目了', '有点久…', '转圈圈…', '马上马上', '快了快了', '别走，就快好了', '在盘了呢', '还在定位', '快复现了'] },
-    { atMs: 60_000, pool: ['1分钟，还在想', '这题有点东西', '让我再钻研下', '嗯…问题不简单', '1分钟，别走开', '盘得有点深', '脑细胞在燃烧', '等等，快盘清了', '还在努力…', '这个有点绕…', '烧脑中…', '别走，快了', '一分钟了，再等等', '这题值得盘', '还在排查', '这个有点复杂'] },
-    { atMs: 300_000, pool: ['5分钟，大工程', '这把我得认真', '确实有点绕', '等等，我在修仙', '快好了，真的', '盘了一大圈', '别慌，在收尾', '给我一首歌的时间', '还没放弃…', '这题真的硬…', '我给跪了…', '憋大招中', '5分钟了，等值了', '快了，真快了', '这个需求很简单', '能跑就别动'] },
-];
+  { atMs: 30_000, pool: ['hmm, thinking harder', '30s and still at it', 'wait, almost', 'hold on, nearly there', 'let me walk it again', 'hmm… still on it', '30s, close', 'wait, a lead', 'taking a while…', 'spinning…', 'almost, almost', 'soon', 'stay, nearly done', 'still working it', 'still locating', 'almost reproduced'] },
+  { atMs: 60_000, pool: ['1 minute, still thinking', 'this one has teeth', 'digging deeper', 'hmm… not simple', '1 minute, stay with me', 'going deep', 'brain cells burning', 'wait, almost clear', 'still going…', 'this is twisty…', 'brain melt…', 'stay, close', 'a minute in, hang on', 'worth the think', 'still tracing', 'this is complex'] },
+  { atMs: 300_000, pool: ['5 minutes, big job', 'taking this seriously', 'yeah this is twisty', 'still in the cave', 'almost, really', 'walked a long loop', 'wrapping up, hang on', 'give me a song', 'not giving up…', 'this one is hard…', 'I am on my knees…', 'charging the big move', '5 minutes, earned it', 'soon, for real', 'simple they said', 'if it runs, leave it'] },
+]
+
 /** Phrases shown while waiting for the first streamed token. */
 export const WAITING_PHRASES = [
-    '呼叫模型…', '模型在路上了', '等它开口…', '稍等，它有点慢', '模型加载中', '嗯…等它一下',
-    '它在组织语言', '等等我嘛', '模型醒了么', '等它伸懒腰', '它打了个哈欠', '模型：来了来了',
-    '等它出字', '别急，在等', '它磨蹭呢', '模型说等一下', '等它滴一声', '模型在咕噜',
-    '等它反应过来', '嗯…等它', '模型在喝水', '它说再等一下', '等它喘口气', '模型：快了快了',
-    '别急别急', '来了来了', '等它跑完', '还在排队', '马上出结果', '等它热身', '模型在酝酿',
-    '它翻了个身', '模型：马上', '等它开机', '它卡了一下', '模型在冥想', '等它眨个眼',
-    '它说稍等', '模型在查资料', '等它缓一缓', '模型在数数', '等它回神', '它终于动了',
-];
+  'calling the model…', 'model on the way', 'waiting for it to speak…', 'hang on, it is slow', 'model loading', 'hmm… waiting',
+  'it is finding words', 'wait for me', 'is the model awake?', 'it is stretching', 'it yawned', 'model: coming',
+  'waiting for tokens', 'hold on', 'it is stalling', 'model said wait', 'waiting for the beep', 'model rumbling',
+  'waiting for it to click', 'hmm… waiting', 'model grabbing a drink', 'it said one more sec', 'let it breathe', 'model: almost',
+  'easy, easy', 'coming, coming', 'waiting for it to finish', 'still in queue', 'result any second', 'warming up', 'model brewing',
+  'it rolled over', 'model: soon', 'waiting for boot', 'it hiccuped', 'model meditating', 'waiting for a blink',
+  'it said hang on', 'model looking things up', 'let it settle', 'model counting', 'waiting for focus', 'it finally moved',
+]
+
 /** Tool-name patterns mapped to playful action verbs. */
 export const ACTION_MAP = [
-    { test: /^(read|read_file|cat)$/i, actions: ['翻翻文档', '让我康康', '读一下', '看一眼', '翻阅中', '读读看', '翻翻', '看看', '瞄一眼', '康康', '翻一页'] },
-    { test: /^(write|write_file|create_file)$/i, actions: ['写写写', '下笔中', '码字呢', '写一段', '记录一下', '写一下', '记下来', '落笔', '开写', '存个文件'] },
-    { test: /^(edit|edit_file|str_replace|apply_patch|search_replace)$/i, actions: ['改改', '修修补补', '润色一下', '编辑中', '调整调整', '改一改', '修一下', '改两行', '调一下', '补一刀'] },
-    { test: /^(bash|shell|run|exec|powershell|cmd)$/i, actions: ['跑个命令', 'bash一下', '敲敲指令', '命令行走起', '执行一下', '敲回车', '跑一下', '敲个命令', '跑命令', '使唤终端'] },
-    { test: /^(grep|rg|search|search_in_files)$/i, actions: ['搜搜东西', 'grep 一下', '找找匹配', '关键词走你', '过滤中', '搜搜看', '搜一下', '找找', '扫一眼', '挖一挖'] },
-    { test: /^(find|glob)$/i, actions: ['找找文件', '找一下', '寻宝中', '找啊找', '文件在哪', '查找中', '搜搜目录'] },
-    { test: /^(ls|list_dir|list)$/i, actions: ['列个清单', '看看目录', 'ls 看一眼', '瞄一下文件', '目录走起', '列出来', '列一下', '瞟一眼', '翻翻'] },
-    { test: /^(web_search|search_web|brave|tavily|exa)$/i, actions: ['网上搜搜', '搜一下', '网络冲浪', '查找资料', '上网瞄瞄', '上网搜搜', '查查', '搜一圈', '打听一下'] },
-    { test: /^(web_fetch|fetch|fetch_content)$/i, actions: ['抓个页面', '拉取一下', 'fetch 中', '扒拉网页', '取点内容', '抓取资料', '扒一下', '打开看看'] },
-    { test: /^(mcp)/i, actions: ['mcp 连一下', '调个服务', '接个工具', 'mcp 走你', '调接口', '连一下', '喊外援', '接一下'] },
-    { test: /^(subagent|agent|task)$/i, actions: ['派个小弟', '小助手出动', '支个 agent', '让小弟跑腿', '代理干活', '子任务起飞', '分个任务', '交给小弟', '派出去'] },
-    { test: /^(todo|manage_todo_list)$/i, actions: ['列个待办', '写个清单', 'todo 安排', '记一下', '待办走起', '清单一下', '记个待办', '打个勾'] },
-    { test: /^(browser|chrome|playwright)/i, actions: ['开个浏览器', '浏览器跑腿', '网页操作', '浏览器干活', '开网页', '点点页面'] },
-    { test: /^(git|gh|github)/i, actions: ['git 操作', '提交一下', '版本控制', 'git 走你', '提交代码', '管个仓库', 'git 一下'] },
-    { test: /^(ask_user_question|ask)$/i, actions: ['提问中', '问一个问题', 'ask 一下', '请教一下', '问问看', '问你个事', '确认一下'] },
-    { test: /^(goal_complete|goal_blocked)$/i, actions: ['定个目标', '设定目标', 'goal 设置', '目标走起', '规划一下', '更新进度'] },
-    { test: /^(todo_write)$/i, actions: ['记个待办', '划个清单', '打个勾'] },
-];
+  { test: /^(read|read_file|cat)$/i, actions: ['reading', 'let me look', 'opening', 'peeking', 'flipping through', 'reading it', 'skimming', 'looking', 'a glance', 'checking', 'one page'] },
+  { test: /^(write|write_file|create_file)$/i, actions: ['writing', 'putting it down', 'typing', 'drafting', 'noting it', 'writing it', 'saving it', 'putting pen down', 'starting a file', 'saving a file'] },
+  { test: /^(edit|edit_file|str_replace|apply_patch|search_replace)$/i, actions: ['editing', 'patching', 'polishing', 'editing now', 'tweaking', 'changing', 'fixing a bit', 'a couple of lines', 'adjusting', 'one more cut'] },
+  { test: /^(bash|shell|run|exec|powershell|cmd)$/i, actions: ['running a command', 'bashing it', 'typing a command', 'command walking', 'executing', 'hitting enter', 'running it', 'a shell command', 'running cmd', 'poking the terminal'] },
+  { test: /^(grep|rg|search|search_in_files)$/i, actions: ['searching', 'grepping', 'finding matches', 'keywords go', 'filtering', 'searching around', 'a search', 'looking for it', 'a scan', 'digging'] },
+  { test: /^(find|glob)$/i, actions: ['finding files', 'looking for a file', 'treasure hunt', 'searching files', 'where is that file', 'finding', 'searching the tree'] },
+  { test: /^(ls|list_dir|list)$/i, actions: ['listing', 'checking the dir', 'ls peek', 'glancing at files', 'directory walk', 'listing them', 'a listing', 'a peek', 'flipping'] },
+  { test: /^(web_search|search_web|brave|tavily|exa)$/i, actions: ['searching the web', 'a web search', 'surfing', 'looking up sources', 'a web peek', 'searching online', 'checking', 'a pass around', 'asking around'] },
+  { test: /^(web_fetch|fetch|fetch_content)$/i, actions: ['fetching a page', 'pulling it', 'fetching', 'grabbing the page', 'taking some content', 'fetching sources', 'a grab', 'opening it'] },
+  { test: /^(mcp)/i, actions: ['calling mcp', 'hitting a service', 'using a tool', 'mcp go', 'calling an API', 'connecting', 'calling backup', 'plugging in'] },
+  { test: /^(subagent|agent|task)$/i, actions: ['sending a helper', 'helper out', 'spinning an agent', 'errand time', 'delegate working', 'subtask launch', 'splitting a task', 'handing it off', 'sending it out'] },
+  { test: /^(todo|manage_todo_list)$/i, actions: ['making a todo', 'writing a list', 'todo time', 'noting it', 'todos go', 'a checklist', 'a todo', 'checking a box'] },
+  { test: /^(browser|chrome|playwright)/i, actions: ['opening a browser', 'browser errand', 'page work', 'browser working', 'opening a page', 'clicking around'] },
+  { test: /^(git|gh|github)/i, actions: ['git work', 'committing', 'version control', 'git go', 'pushing code', 'tending the repo', 'a git pass'] },
+  { test: /^(ask_user_question|ask)$/i, actions: ['asking', 'asking a question', 'asking you', 'checking with you', 'a question', 'one thing', 'confirming'] },
+  { test: /^(goal_complete|goal_blocked)$/i, actions: ['setting a goal', 'goal set', 'goal time', 'goals go', 'planning', 'updating progress'] },
+  { test: /^(todo_write)$/i, actions: ['writing a todo', 'a checklist', 'checking a box'] },
+]
+
 /** Fallback verbs for unknown tools. */
-export const FALLBACK_ACTIONS = ['干活', '调用', '整一下', '搞一下', '动动手', '备选方案', '换条路'];
+export const FALLBACK_ACTIONS = ['working', 'calling', 'doing a thing', 'getting it done', 'hands on', 'fallback', 'another path']
+
 /** Tool failure phrases, replacing a bare ✗. */
 export const FAIL_PHRASES = [
-    '翻车了', '哎呀', '掉了', '没跑通', '摔了一跤', '再来一次', '这不对', '出岔子了', '不灵了',
-    '坏消息', '权限不对？', '连不上？', '404了', '不太对', '有点问题', '再看看', '没接住', '漏了',
-    '我本地能跑啊', '昨天还能跑', '重启试试', '清一下缓存', '删了重装', '你刷新一下', '环境问题',
-    '少了个分号', '拼错了', '没保存', '又不是不能用', '绷不住了', '难绷', '卒', '裂开',
-    '血压上来了', '缓存害我', '再给我一次机会', '这波大意了', '手滑', '回滚重来', '换个姿势',
-];
+  'it crashed', 'oops', 'dropped it', 'did not run', 'tripped', 'try again', 'that is wrong', 'went sideways', 'nope',
+  'bad news', 'permissions?', 'cannot connect?', '404', 'not quite', 'something is off', 'look again', 'missed it', 'leaked',
+  'works on my machine', 'it ran yesterday', 'try a restart', 'clear the cache', 'delete and reinstall', 'refresh it', 'env issue',
+  'missing a semicolon', 'typo', 'did not save', 'it still kind of works', 'cannot even', 'oof', 'dead', 'split',
+  'blood pressure up', 'cache got me', 'one more chance', 'that was sloppy', 'slip', 'rollback', 'try another angle',
+]
+
 /** Turn-completion phrases. */
 export const DONE_PHRASES = [
-    '交差！', '搞定，下一个', '好了，收工', '完成啦', '交作业', '结束，完美', '完工咯', '搞定啦',
-    '任务完成', '好了，歇会儿', '搞定', '收工', '妥了', '完事', '交差', '齐活', '拿下', '收工！',
-    '搞定收工', '收！', '完事！', '下一题', '能跑！', '没报错', '过了', '上线！', '稳了', '6',
-    '完工！', '完美收场', '这波不亏', '一次过', '收工摸鱼', '漂亮', '全绿', '干净利落',
-    '手到擒来', '水到渠成', '下班！', '歇口气', '交接完成', '工单关闭', '收尾完毕',
-];
+  'shipped!', 'done, next', 'all right, wrap', 'done', 'homework in', 'finished, nice', 'wrapped', 'got it',
+  'task complete', 'ok, a breather', 'done', 'clocked out', 'sorted', 'finished', 'handed in', 'all set', 'nailed it', 'done!',
+  'done and dusted', 'in!', 'finished!', 'next up', 'it runs!', 'no errors', 'passed', 'ship it!', 'solid', '6',
+  'complete!', 'clean finish', 'worth it', 'first try', 'done, slack time', 'nice', 'all green', 'clean',
+  'easy as that', 'fell into place', 'quitting time!', 'a breath', 'handoff done', 'ticket closed', 'wrapped up',
+]
+
 /** Night-owl phrases mixed in between 00:00 and 06:00 local time. */
 export const NIGHT_PHRASES = [
-    '修仙中…', '深夜冒泡', '你也是夜猫子呀', '月亮不睡我不睡', '夜里脑子慢，谅解', '晚安？还早呢',
-    '深夜盘东西', '熬夜冠军上线', '困了，但能行', '过了零点照样肝', '夜猫子出没', '深夜档营业',
-    '星星都睡了', '凌晨还在盘', '深夜上线', '凌晨部署', '通宵了',
-];
+  'late-night grind…', 'midnight ping', 'night owl too, huh', 'the moon is still up', 'brain is slow at night, sorry', 'good night? still early',
+  'late-night work', 'all-nighter champion', 'sleepy, but ok', 'past midnight and still going', 'night owl on duty', 'late shift open',
+  'the stars went to bed', 'still at it at dawn', 'late-night ship', 'deploying at 3am', 'all-nighter',
+]
+
 /** Common git tool names / bash commands containing `git `. */
-export const GIT_TOOL_RE = /^(?:git|git_diff|git_commit|git_push|git_pull|git_checkout|git_branch|git_merge|git_rebase|github|gh)$/i;
+export const GIT_TOOL_RE = /^(?:git|git_diff|git_commit|git_push|git_pull|git_checkout|git_branch|git_merge|git_rebase|github|gh)$/i
+
 /** Detect the 00:00–06:00 night window (local time). */
 export function isNight(hour) {
-    return hour >= 0 && hour < 6;
+  return hour >= 0 && hour < 6
 }
+
 /**
  * Pick a thinking phrase appropriate for the elapsed thinking time.
  * @param elapsedMs - Milliseconds spent thinking in the current phase.
@@ -109,55 +120,52 @@ export function isNight(hour) {
  * @param night - Mix night-owl copy into the pool.
  */
 export function thinkingPhrase(elapsedMs, previous, night = false) {
-    let pool = THINKING_PHRASES;
-    for (const tier of THINKING_TIERS) {
-        if (elapsedMs >= tier.atMs) {
-            pool = tier.pool;
-            break;
-        }
-    }
-    if (night && pool === THINKING_PHRASES) {
-        return pickPhrase([...pool, ...NIGHT_PHRASES], previous);
+  let pool = THINKING_PHRASES
+  for (const tier of THINKING_TIERS) {
+    if (elapsedMs >= tier.atMs) {
+      pool = tier.pool
+      break
     }
-    return pickPhrase(pool, previous);
+  }
+  if (night && pool === THINKING_PHRASES) {
+    return pickPhrase([...pool, ...NIGHT_PHRASES], previous)
+  }
+  return pickPhrase(pool, previous)
 }
+
 /**
  * Map a tool name to a playful action verb.
  * @param toolName - Registry tool name (unqualified).
  * @param custom - Exact-name custom action pools, matched case-insensitively.
  */
 export function actionFor(toolName, custom) {
-    const normalized = toolName.trim().toLowerCase();
-    const customPool = custom?.[normalized];
-    if (customPool !== undefined && customPool.length > 0)
-        return pickPhrase(customPool);
-    for (const { test, actions } of ACTION_MAP) {
-        if (test.test(normalized))
-            return pickPhrase(actions);
-    }
-    return pickPhrase(FALLBACK_ACTIONS);
+  const normalized = toolName.trim().toLowerCase()
+  const customPool = custom?.[normalized]
+  if (customPool !== undefined && customPool.length > 0) return pickPhrase(customPool)
+  for (const { test, actions } of ACTION_MAP) {
+    if (test.test(normalized)) return pickPhrase(actions)
+  }
+  return pickPhrase(FALLBACK_ACTIONS)
 }
+
 /** Whether a tool is a git operation (name match, or a shell command containing `git `). */
 export function isGitTool(toolName, args) {
-    if (GIT_TOOL_RE.test(toolName.trim()))
-        return true;
-    if (/^(?:bash|shell|cmd|powershell|pwsh)$/i.test(toolName.trim())) {
-        const command = args?.command ?? args?.cmdline;
-        return typeof command === 'string' && /\bgit\s+/.test(command);
-    }
-    return false;
+  if (GIT_TOOL_RE.test(toolName.trim())) return true
+  if (/^(?:bash|shell|cmd|powershell|pwsh)$/i.test(toolName.trim())) {
+    const command = args?.command ?? args?.cmdline
+    return typeof command === 'string' && /\bgit\s+/.test(command)
+  }
+  return false
 }
+
 /** Format milliseconds as a compact human duration (`1m23s`). */
 export function fmtDuration(ms) {
-    if (ms < 1000)
-        return '0s';
-    const total = Math.floor(ms / 1000);
-    if (total < 60)
-        return `${total}s`;
-    const minutes = Math.floor(total / 60);
-    const seconds = total % 60;
-    if (minutes < 60)
-        return `${minutes}m${seconds}s`;
-    const hours = Math.floor(minutes / 60);
-    return `${hours}h${minutes % 60}m`;
+  if (ms < 1000) return '0s'
+  const total = Math.floor(ms / 1000)
+  if (total < 60) return `${total}s`
+  const minutes = Math.floor(total / 60)
+  const seconds = total % 60
+  if (minutes < 60) return `${minutes}m${seconds}s`
+  const hours = Math.floor(minutes / 60)
+  return `${hours}h${minutes % 60}m`
 }
diff --git a/lib/types/status.js b/lib/types/status.js
index f95903968427512c8164287eaf0cf2cea5c27481..f40d30b978a21f8c8f4146f6ffc434baacefb7c3 100644
--- a/lib/types/status.js
+++ b/lib/types/status.js
@@ -6,7 +6,7 @@
  * @module @deepseek-ai/dsh-working-activity/status
  */
 import { actionFor, fmtDuration, isGitTool, isNight, pickPhrase, thinkingPhrase, WAITING_PHRASES, DONE_PHRASES, FAIL_PHRASES, } from './phrases.js';
-/** Format one tool into its display fragment (`跑个命令 npm test`). */
+/** Format one tool into its display fragment (`run a command npm test`). */
 function toolFragment(tool) {
     return tool.detail.length === 0 ? tool.action : `${tool.action} ${tool.detail}`;
 }
@@ -94,7 +94,7 @@ export class ActivityTracker {
     /** Total tokens reported across the turn's assistant messages. */
     turnTokens = 0;
     /** Completion prefix drawn ONCE at turn end so the done line stays stable. */
-    donePrefix = '搞定 ✓';
+    donePrefix = 'Done ✓';
     /**
      * @param config - Behavioral knobs.
      * @param now - Wall-clock supplier (injectable for tests).
@@ -237,7 +237,7 @@ export class ActivityTracker {
                     this.donePrefix = lastTool?.failed ? pickPhrase(FAIL_PHRASES) : pickPhrase(DONE_PHRASES);
                 }
                 else {
-                    this.donePrefix = '搞定 ✓';
+                    this.donePrefix = 'Done ✓';
                 }
                 this.setPhase('done', at);
                 return;
@@ -318,7 +318,7 @@ export class ActivityTracker {
         if (narration !== null) {
             return {
                 phase: this.phase,
-                line: `⏵ ${narration} · 总${elapsed}`,
+                line: `⏵ ${narration} · total ${elapsed}`,
                 phrase: narration,
                 toolCount: this.toolCount,
                 turnElapsedMs: this.turnElapsedMs(nowMs),
@@ -339,17 +339,17 @@ export class ActivityTracker {
                 : thinkingPhrase(thinkingMs, undefined, isNight(new Date(nowMs).getHours())));
             return {
                 phase: this.phase,
-                line: `${phrase} · 总${elapsed}`,
+                line: `${phrase} · total ${elapsed}`,
                 phrase,
                 toolCount: this.toolCount,
                 turnElapsedMs: this.turnElapsedMs(nowMs),
                 phaseStartedAt: this.phaseStartedAt,
             };
         }
-        const label = this.phase === 'waiting' ? '等待模型响应' : '思考中';
+        const label = this.phase === 'waiting' ? 'Waiting for model' : 'Thinking';
         return {
             phase: this.phase,
-            line: `${label} · 总${elapsed}`,
+            line: `${label} · total ${elapsed}`,
             label,
             toolCount: this.toolCount,
             turnElapsedMs: this.turnElapsedMs(nowMs),
@@ -359,16 +359,16 @@ export class ActivityTracker {
     doneSummary(nowMs) {
         const { thinkingMs, toolMs, toolCount } = this.stats();
         const tokens = this.turnTokens > 0 ? ` · 🔥 ${fmtTokens(this.turnTokens)}` : '';
-        const base = `${this.donePrefix} · ${toolCount} 工具 · 想${fmtDuration(thinkingMs)} 干${fmtDuration(toolMs)}${tokens}`;
+        const base = `${this.donePrefix} · ${toolCount} tools · think ${fmtDuration(thinkingMs)} work ${fmtDuration(toolMs)}${tokens}`;
         if (!this.config.phrases) {
-            return { line: `搞定 ✓ · ${toolCount} 工具 · 想${fmtDuration(thinkingMs)} 干${fmtDuration(toolMs)}${tokens}` };
+            return { line: `Done ✓ · ${toolCount} tools · think ${fmtDuration(thinkingMs)} work ${fmtDuration(toolMs)}${tokens}` };
         }
         const last = this.doneQueue.at(-1);
         if (last !== undefined && nowMs - last.endedAt < DONE_FRAGMENT_MS) {
             const fragment = toolFragment(last);
-            return { line: `${this.donePrefix} · ${fragment} · ${toolCount} 工具${tokens}`, phrase: this.donePrefix };
+            return { line: `${this.donePrefix} · ${fragment} · ${toolCount} tools${tokens}`, phrase: this.donePrefix };
         }
-        return { line: base, ...(this.donePrefix === '搞定 ✓' ? {} : { phrase: this.donePrefix }) };
+        return { line: base, ...(this.donePrefix === 'Done ✓' ? {} : { phrase: this.donePrefix }) };
     }
     /** The fresh self-narration line, or null once the stream has been quiet. */
     freshNarration(nowMs) {
diff --git a/src/client/index.ts b/src/client/index.ts
index 98ee7a2f67498d4d2cf6ea44e5cd5902c1260127..9ab4bc74967e4d97a3c8206ee03e25845112d94c 100644
--- a/src/client/index.ts
+++ b/src/client/index.ts
@@ -6,7 +6,7 @@
  * the live frame through the standard session kit and owns no store, no
  * refresh chain, and no event listener.
  *
- * Mount contract (see the root README's "Web UI 集成" section): the web
+ * Mount contract (see the root README's "Web UI integration" section): the web
  * client's client-modules host scans loader entries for `dsh.client`
  * declarations and serves this package's `./client` bundle at
  * /plugins/dsh-working-activity/client.js. The entry contributes into the
diff --git a/src/index.ts b/src/index.ts
index c22544d78bbcda57b5d406e9898336e79bfe717a..019c23020d5ed8c18d79223350b259f9e03ebc50 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -91,7 +91,7 @@ interface ResolvedConfig {
 
 /** The self-narration contract injected into the system prompt (narrate on). */
 const NARRATE_INSTRUCTION =
-  '[状态栏] 你有一个状态栏展示给用户。【必须】在每个步骤/子任务开始时（不只是调用工具前），在回复正文的最前面单独写一行：⏵ 你在做的具体事情（不超过20字），然后换行继续正常回复。整轮回复只写一行 ⏵，不要重复。信息为主——让人一眼知道你在干什么，风格自然、可以带点俏皮。例：⏵ 修复登录页样式、⏵ 查一下报错原因、⏵ 给补丁跑个验证。切换任务时必须更新。'
+  '[Status bar] You have a status bar shown to the user. You MUST start each step/subtask (not only before tool calls) with a single line at the very top of your reply: ⏵ a short concrete description of what you are doing (at most 20 words), then a newline and your normal reply. Write only one ⏵ line per turn. Keep it informative so the user can see what you are doing. Examples: ⏵ Fix login page styles, ⏵ Check the error cause, ⏵ Run the patch tests. Update the line when you switch tasks. Always write the ⏵ line in English.'
 
 /**
  * Wire the working-activity plugin.
diff --git a/src/phrases.ts b/src/phrases.ts
index 354b8afd0a7de04091db680e54debde44e8bf7f2..66a2330660dcc758f3d23a878ba66eb8727e2ea9 100644
--- a/src/phrases.ts
+++ b/src/phrases.ts
@@ -1,7 +1,7 @@
 /**
  * Copy pools for the working-activity status line: short, colloquial, playful
- * Chinese fragments with deadpan English one-liners mixed in, matching the
- * pi-working-activity tone. Everything here is pure data + pure pickers.
+ * English fragments, matching the pi-working-activity tone. Everything here
+ * is pure data + pure pickers.
  * @module @deepseek-ai/dsh-working-activity/phrases
  */
 
@@ -22,22 +22,22 @@ export function pickPhrase(entries: PhrasePool, previous?: string): string {
 
 /** Thinking phrases while the model works without a tool. */
 export const THINKING_PHRASES: readonly string[] = [
-  '嗯…让我捋捋', '盘一下盘一下', '大脑转起来了', '思考.gif', '给我一秒', '脑子在冒烟',
-  '想呢想呢', '别催别催', '啾，让我想想', '让我琢磨下', '嗯…等一下哦', '正在盘逻辑',
-  '小脑瓜动一下', '嗯？哦…', '让我理理', '翻翻脑子', '回想中', '等一下下', '让我嗅嗅',
-  '脑内风暴中', '嗯…让我品品', '滴滴滴思考中', '稍等，在想', '盘明白了么', '挠头…',
-  '让子弹飞一会', '让我脑补一下', '加载中', '你说 我在听', '噢…是这样', '让我嚼一嚼',
-  '嗯…有点意思', '搓搓手想想', '等下，在想', '让我康康', '想好了告诉你', '脑子转圈圈',
-  '嗯…让我反应下', '等下下嘛', '思路加载中', '琢磨中', '嗯…让我拆一下', '盘，都可以盘',
-  '让我嗅探一下', '脑内跑火车', '嗯…让我缓一下', '滴滴，想呢', '思索.jpg', '嗯…有点东西',
-  '让我品', '小跑一下思路', '等下，有画面了', '让我咀嚼', '嗯…发会儿呆', '思考泡泡',
-  '脑电波传输中', '嗯…转转', '等下，盘好了', '让我回味', '滴滴滴', '思考的鱼',
-  '嗯…让我摸一下', '脑子在煮咖啡', '等下，我打个腹稿', '嗯…重启一下', '让我挠墙',
-  '嗯，来了来了', '脑子冒泡泡', '嗯…有点烫', '思考猫猫', '让我咕噜一下', '嗯…盘它',
-  '等下，我闪个思路', '脑子在蹦迪', '嗯…', '让我想想', '盘一下', '啾', 'lol', 'hm', 'oh',
+  'hmm, let me think', 'working it out', 'brain spinning up', 'thinking.gif', 'one sec', 'wheels turning',
+  'thinking…', 'hold on', 'let me see', 'mulling it over', 'hang on a sec', 'sorting the logic',
+  'gears turning', 'hmm? oh…', 'lining it up', 'poking around', 'recalling', 'just a moment', 'sniffing this out',
+  'storm in progress', 'hmm, tasting that', 'thinking… beep', 'wait, thinking', 'figured it yet?', 'scratching my head…',
+  'let it cook', 'filling in the picture', 'loading', 'I hear you', 'oh… got it', 'chewing on it',
+  'hmm, interesting', 'rubbing hands', 'wait, thinking', 'let me look', 'I will tell you when I have it', 'spinning',
+  'hmm, processing', 'one more sec', 'loading the idea', 'mulling', 'hmm, breaking it down', 'let me work this',
+  'sniffing around', 'train of thought', 'hmm, give me a beat', 'beep, thinking', 'ponder.jpg', 'hmm, something here',
+  'let me taste that', 'jogging the thought', 'wait, picture forming', 'chewing', 'hmm, zoning out', 'think bubbles',
+  'brainwaves incoming', 'hmm, spinning', 'wait, almost there', 'savoring it', 'beep beep', 'thinking fish',
+  'hmm, poking it', 'brain on coffee', 'wait, drafting', 'hmm, reboot', 'climbing the wall',
+  'ok, coming', 'brain bubbling', 'hmm, a bit hot', 'thinking cat', 'let me rumble', 'hmm, work it',
+  'wait, a spark', 'brain on disco', 'hmm…', 'let me think', 'work it', 'hey', 'lol', 'hm', 'oh',
   'ok', 'um', 'heh', 'uh', 'nah', 'mm', 'wow', 'nice', 'rgrg', 'okk', 'hhh', 'emm', 'emmm',
-  'CPU烧了', '让我打个log看看', '先跑一下试试', '定位一下', '排查一下', '看看日志',
-  'loading 99%', '让我捋一下逻辑',
+  'CPU smoking', 'let me check a log', 'try a run first', 'pin it down', 'dig into it', 'check the logs',
+  'loading 99%', 'let me straighten the logic',
 ]
 
 /** Tiered phrases when thinking runs long (elapsed >= threshold). */
@@ -46,20 +46,20 @@ export const THINKING_TIERS: readonly {
   readonly atMs: number
   readonly pool: readonly string[]
 }[] = [
-  { atMs: 30_000, pool: ['嗯，让我细想想', '30秒了，还在盘', '等下，快好了', '别急，就快出结果了', '让我再捋一捋', '嗯…思路没断', '30秒，快了', '等等，有眉目了', '有点久…', '转圈圈…', '马上马上', '快了快了', '别走，就快好了', '在盘了呢', '还在定位', '快复现了'] },
-  { atMs: 60_000, pool: ['1分钟，还在想', '这题有点东西', '让我再钻研下', '嗯…问题不简单', '1分钟，别走开', '盘得有点深', '脑细胞在燃烧', '等等，快盘清了', '还在努力…', '这个有点绕…', '烧脑中…', '别走，快了', '一分钟了，再等等', '这题值得盘', '还在排查', '这个有点复杂'] },
-  { atMs: 300_000, pool: ['5分钟，大工程', '这把我得认真', '确实有点绕', '等等，我在修仙', '快好了，真的', '盘了一大圈', '别慌，在收尾', '给我一首歌的时间', '还没放弃…', '这题真的硬…', '我给跪了…', '憋大招中', '5分钟了，等值了', '快了，真快了', '这个需求很简单', '能跑就别动'] },
+  { atMs: 30_000, pool: ['hmm, thinking harder', '30s and still at it', 'wait, almost', 'hold on, nearly there', 'let me walk it again', 'hmm… still on it', '30s, close', 'wait, a lead', 'taking a while…', 'spinning…', 'almost, almost', 'soon', 'stay, nearly done', 'still working it', 'still locating', 'almost reproduced'] },
+  { atMs: 60_000, pool: ['1 minute, still thinking', 'this one has teeth', 'digging deeper', 'hmm… not simple', '1 minute, stay with me', 'going deep', 'brain cells burning', 'wait, almost clear', 'still going…', 'this is twisty…', 'brain melt…', 'stay, close', 'a minute in, hang on', 'worth the think', 'still tracing', 'this is complex'] },
+  { atMs: 300_000, pool: ['5 minutes, big job', 'taking this seriously', 'yeah this is twisty', 'still in the cave', 'almost, really', 'walked a long loop', 'wrapping up, hang on', 'give me a song', 'not giving up…', 'this one is hard…', 'I am on my knees…', 'charging the big move', '5 minutes, earned it', 'soon, for real', 'simple they said', 'if it runs, leave it'] },
 ]
 
 /** Phrases shown while waiting for the first streamed token. */
 export const WAITING_PHRASES: readonly string[] = [
-  '呼叫模型…', '模型在路上了', '等它开口…', '稍等，它有点慢', '模型加载中', '嗯…等它一下',
-  '它在组织语言', '等等我嘛', '模型醒了么', '等它伸懒腰', '它打了个哈欠', '模型：来了来了',
-  '等它出字', '别急，在等', '它磨蹭呢', '模型说等一下', '等它滴一声', '模型在咕噜',
-  '等它反应过来', '嗯…等它', '模型在喝水', '它说再等一下', '等它喘口气', '模型：快了快了',
-  '别急别急', '来了来了', '等它跑完', '还在排队', '马上出结果', '等它热身', '模型在酝酿',
-  '它翻了个身', '模型：马上', '等它开机', '它卡了一下', '模型在冥想', '等它眨个眼',
-  '它说稍等', '模型在查资料', '等它缓一缓', '模型在数数', '等它回神', '它终于动了',
+  'calling the model…', 'model on the way', 'waiting for it to speak…', 'hang on, it is slow', 'model loading', 'hmm… waiting',
+  'it is finding words', 'wait for me', 'is the model awake?', 'it is stretching', 'it yawned', 'model: coming',
+  'waiting for tokens', 'hold on', 'it is stalling', 'model said wait', 'waiting for the beep', 'model rumbling',
+  'waiting for it to click', 'hmm… waiting', 'model grabbing a drink', 'it said one more sec', 'let it breathe', 'model: almost',
+  'easy, easy', 'coming, coming', 'waiting for it to finish', 'still in queue', 'result any second', 'warming up', 'model brewing',
+  'it rolled over', 'model: soon', 'waiting for boot', 'it hiccuped', 'model meditating', 'waiting for a blink',
+  'it said hang on', 'model looking things up', 'let it settle', 'model counting', 'waiting for focus', 'it finally moved',
 ]
 
 /** Tool-name patterns mapped to playful action verbs. */
@@ -67,51 +67,51 @@ export const ACTION_MAP: readonly {
   readonly test: RegExp
   readonly actions: readonly string[]
 }[] = [
-  { test: /^(read|read_file|cat)$/i, actions: ['翻翻文档', '让我康康', '读一下', '看一眼', '翻阅中', '读读看', '翻翻', '看看', '瞄一眼', '康康', '翻一页'] },
-  { test: /^(write|write_file|create_file)$/i, actions: ['写写写', '下笔中', '码字呢', '写一段', '记录一下', '写一下', '记下来', '落笔', '开写', '存个文件'] },
-  { test: /^(edit|edit_file|str_replace|apply_patch|search_replace)$/i, actions: ['改改', '修修补补', '润色一下', '编辑中', '调整调整', '改一改', '修一下', '改两行', '调一下', '补一刀'] },
-  { test: /^(bash|shell|run|exec|powershell|cmd)$/i, actions: ['跑个命令', 'bash一下', '敲敲指令', '命令行走起', '执行一下', '敲回车', '跑一下', '敲个命令', '跑命令', '使唤终端'] },
-  { test: /^(grep|rg|search|search_in_files)$/i, actions: ['搜搜东西', 'grep 一下', '找找匹配', '关键词走你', '过滤中', '搜搜看', '搜一下', '找找', '扫一眼', '挖一挖'] },
-  { test: /^(find|glob)$/i, actions: ['找找文件', '找一下', '寻宝中', '找啊找', '文件在哪', '查找中', '搜搜目录'] },
-  { test: /^(ls|list_dir|list)$/i, actions: ['列个清单', '看看目录', 'ls 看一眼', '瞄一下文件', '目录走起', '列出来', '列一下', '瞟一眼', '翻翻'] },
-  { test: /^(web_search|search_web|brave|tavily|exa)$/i, actions: ['网上搜搜', '搜一下', '网络冲浪', '查找资料', '上网瞄瞄', '上网搜搜', '查查', '搜一圈', '打听一下'] },
-  { test: /^(web_fetch|fetch|fetch_content)$/i, actions: ['抓个页面', '拉取一下', 'fetch 中', '扒拉网页', '取点内容', '抓取资料', '扒一下', '打开看看'] },
-  { test: /^(mcp)/i, actions: ['mcp 连一下', '调个服务', '接个工具', 'mcp 走你', '调接口', '连一下', '喊外援', '接一下'] },
-  { test: /^(subagent|agent|task)$/i, actions: ['派个小弟', '小助手出动', '支个 agent', '让小弟跑腿', '代理干活', '子任务起飞', '分个任务', '交给小弟', '派出去'] },
-  { test: /^(todo|manage_todo_list)$/i, actions: ['列个待办', '写个清单', 'todo 安排', '记一下', '待办走起', '清单一下', '记个待办', '打个勾'] },
-  { test: /^(browser|chrome|playwright)/i, actions: ['开个浏览器', '浏览器跑腿', '网页操作', '浏览器干活', '开网页', '点点页面'] },
-  { test: /^(git|gh|github)/i, actions: ['git 操作', '提交一下', '版本控制', 'git 走你', '提交代码', '管个仓库', 'git 一下'] },
-  { test: /^(ask_user_question|ask)$/i, actions: ['提问中', '问一个问题', 'ask 一下', '请教一下', '问问看', '问你个事', '确认一下'] },
-  { test: /^(goal_complete|goal_blocked)$/i, actions: ['定个目标', '设定目标', 'goal 设置', '目标走起', '规划一下', '更新进度'] },
-  { test: /^(todo_write)$/i, actions: ['记个待办', '划个清单', '打个勾'] },
+  { test: /^(read|read_file|cat)$/i, actions: ['reading', 'let me look', 'opening', 'peeking', 'flipping through', 'reading it', 'skimming', 'looking', 'a glance', 'checking', 'one page'] },
+  { test: /^(write|write_file|create_file)$/i, actions: ['writing', 'putting it down', 'typing', 'drafting', 'noting it', 'writing it', 'saving it', 'putting pen down', 'starting a file', 'saving a file'] },
+  { test: /^(edit|edit_file|str_replace|apply_patch|search_replace)$/i, actions: ['editing', 'patching', 'polishing', 'editing now', 'tweaking', 'changing', 'fixing a bit', 'a couple of lines', 'adjusting', 'one more cut'] },
+  { test: /^(bash|shell|run|exec|powershell|cmd)$/i, actions: ['running a command', 'bashing it', 'typing a command', 'command walking', 'executing', 'hitting enter', 'running it', 'a shell command', 'running cmd', 'poking the terminal'] },
+  { test: /^(grep|rg|search|search_in_files)$/i, actions: ['searching', 'grepping', 'finding matches', 'keywords go', 'filtering', 'searching around', 'a search', 'looking for it', 'a scan', 'digging'] },
+  { test: /^(find|glob)$/i, actions: ['finding files', 'looking for a file', 'treasure hunt', 'searching files', 'where is that file', 'finding', 'searching the tree'] },
+  { test: /^(ls|list_dir|list)$/i, actions: ['listing', 'checking the dir', 'ls peek', 'glancing at files', 'directory walk', 'listing them', 'a listing', 'a peek', 'flipping'] },
+  { test: /^(web_search|search_web|brave|tavily|exa)$/i, actions: ['searching the web', 'a web search', 'surfing', 'looking up sources', 'a web peek', 'searching online', 'checking', 'a pass around', 'asking around'] },
+  { test: /^(web_fetch|fetch|fetch_content)$/i, actions: ['fetching a page', 'pulling it', 'fetching', 'grabbing the page', 'taking some content', 'fetching sources', 'a grab', 'opening it'] },
+  { test: /^(mcp)/i, actions: ['calling mcp', 'hitting a service', 'using a tool', 'mcp go', 'calling an API', 'connecting', 'calling backup', 'plugging in'] },
+  { test: /^(subagent|agent|task)$/i, actions: ['sending a helper', 'helper out', 'spinning an agent', 'errand time', 'delegate working', 'subtask launch', 'splitting a task', 'handing it off', 'sending it out'] },
+  { test: /^(todo|manage_todo_list)$/i, actions: ['making a todo', 'writing a list', 'todo time', 'noting it', 'todos go', 'a checklist', 'a todo', 'checking a box'] },
+  { test: /^(browser|chrome|playwright)/i, actions: ['opening a browser', 'browser errand', 'page work', 'browser working', 'opening a page', 'clicking around'] },
+  { test: /^(git|gh|github)/i, actions: ['git work', 'committing', 'version control', 'git go', 'pushing code', 'tending the repo', 'a git pass'] },
+  { test: /^(ask_user_question|ask)$/i, actions: ['asking', 'asking a question', 'asking you', 'checking with you', 'a question', 'one thing', 'confirming'] },
+  { test: /^(goal_complete|goal_blocked)$/i, actions: ['setting a goal', 'goal set', 'goal time', 'goals go', 'planning', 'updating progress'] },
+  { test: /^(todo_write)$/i, actions: ['writing a todo', 'a checklist', 'checking a box'] },
 ]
 
 /** Fallback verbs for unknown tools. */
-export const FALLBACK_ACTIONS: readonly string[] = ['干活', '调用', '整一下', '搞一下', '动动手', '备选方案', '换条路']
+export const FALLBACK_ACTIONS: readonly string[] = ['working', 'calling', 'doing a thing', 'getting it done', 'hands on', 'fallback', 'another path']
 
 /** Tool failure phrases, replacing a bare ✗. */
 export const FAIL_PHRASES: readonly string[] = [
-  '翻车了', '哎呀', '掉了', '没跑通', '摔了一跤', '再来一次', '这不对', '出岔子了', '不灵了',
-  '坏消息', '权限不对？', '连不上？', '404了', '不太对', '有点问题', '再看看', '没接住', '漏了',
-  '我本地能跑啊', '昨天还能跑', '重启试试', '清一下缓存', '删了重装', '你刷新一下', '环境问题',
-  '少了个分号', '拼错了', '没保存', '又不是不能用', '绷不住了', '难绷', '卒', '裂开',
-  '血压上来了', '缓存害我', '再给我一次机会', '这波大意了', '手滑', '回滚重来', '换个姿势',
+  'it crashed', 'oops', 'dropped it', 'did not run', 'tripped', 'try again', 'that is wrong', 'went sideways', 'nope',
+  'bad news', 'permissions?', 'cannot connect?', '404', 'not quite', 'something is off', 'look again', 'missed it', 'leaked',
+  'works on my machine', 'it ran yesterday', 'try a restart', 'clear the cache', 'delete and reinstall', 'refresh it', 'env issue',
+  'missing a semicolon', 'typo', 'did not save', 'it still kind of works', 'cannot even', 'oof', 'dead', 'split',
+  'blood pressure up', 'cache got me', 'one more chance', 'that was sloppy', 'slip', 'rollback', 'try another angle',
 ]
 
 /** Turn-completion phrases. */
 export const DONE_PHRASES: readonly string[] = [
-  '交差！', '搞定，下一个', '好了，收工', '完成啦', '交作业', '结束，完美', '完工咯', '搞定啦',
-  '任务完成', '好了，歇会儿', '搞定', '收工', '妥了', '完事', '交差', '齐活', '拿下', '收工！',
-  '搞定收工', '收！', '完事！', '下一题', '能跑！', '没报错', '过了', '上线！', '稳了', '6',
-  '完工！', '完美收场', '这波不亏', '一次过', '收工摸鱼', '漂亮', '全绿', '干净利落',
-  '手到擒来', '水到渠成', '下班！', '歇口气', '交接完成', '工单关闭', '收尾完毕',
+  'shipped!', 'done, next', 'all right, wrap', 'done', 'homework in', 'finished, nice', 'wrapped', 'got it',
+  'task complete', 'ok, a breather', 'done', 'clocked out', 'sorted', 'finished', 'handed in', 'all set', 'nailed it', 'done!',
+  'done and dusted', 'in!', 'finished!', 'next up', 'it runs!', 'no errors', 'passed', 'ship it!', 'solid', '6',
+  'complete!', 'clean finish', 'worth it', 'first try', 'done, slack time', 'nice', 'all green', 'clean',
+  'easy as that', 'fell into place', 'quitting time!', 'a breath', 'handoff done', 'ticket closed', 'wrapped up',
 ]
 
 /** Night-owl phrases mixed in between 00:00 and 06:00 local time. */
 export const NIGHT_PHRASES: readonly string[] = [
-  '修仙中…', '深夜冒泡', '你也是夜猫子呀', '月亮不睡我不睡', '夜里脑子慢，谅解', '晚安？还早呢',
-  '深夜盘东西', '熬夜冠军上线', '困了，但能行', '过了零点照样肝', '夜猫子出没', '深夜档营业',
-  '星星都睡了', '凌晨还在盘', '深夜上线', '凌晨部署', '通宵了',
+  'late-night grind…', 'midnight ping', 'night owl too, huh', 'the moon is still up', 'brain is slow at night, sorry', 'good night? still early',
+  'late-night work', 'all-nighter champion', 'sleepy, but ok', 'past midnight and still going', 'night owl on duty', 'late shift open',
+  'the stars went to bed', 'still at it at dawn', 'late-night ship', 'deploying at 3am', 'all-nighter',
 ]
 
 /** Common git tool names / bash commands containing `git `. */
diff --git a/src/status.ts b/src/status.ts
index 6848cedbddfa4937f04de3165b733c3e474becc0..d65a74968e34a6c190d3de54608c898098a6cafa 100644
--- a/src/status.ts
+++ b/src/status.ts
@@ -77,7 +77,7 @@ interface DoneTool {
   readonly endedAt: number
 }
 
-/** Format one tool into its display fragment (`跑个命令 npm test`). */
+/** Format one tool into its display fragment (`run a command npm test`). */
 function toolFragment(tool: { action: string; detail: string }): string {
   return tool.detail.length === 0 ? tool.action : `${tool.action} ${tool.detail}`
 }
@@ -155,7 +155,7 @@ export class ActivityTracker {
   /** Total tokens reported across the turn's assistant messages. */
   private turnTokens = 0
   /** Completion prefix drawn ONCE at turn end so the done line stays stable. */
-  private donePrefix = '搞定 ✓'
+  private donePrefix = 'Done ✓'
 
   /**
    * @param config - Behavioral knobs.
@@ -295,7 +295,7 @@ export class ActivityTracker {
         if (this.config.phrases) {
           this.donePrefix = lastTool?.failed ? pickPhrase(FAIL_PHRASES) : pickPhrase(DONE_PHRASES)
         } else {
-          this.donePrefix = '搞定 ✓'
+          this.donePrefix = 'Done ✓'
         }
         this.setPhase('done', at)
         return
@@ -379,7 +379,7 @@ export class ActivityTracker {
     if (narration !== null) {
       return {
         phase: this.phase,
-        line: `⏵ ${narration} · 总${elapsed}`,
+        line: `⏵ ${narration} · total ${elapsed}`,
         phrase: narration,
         toolCount: this.toolCount,
         turnElapsedMs: this.turnElapsedMs(nowMs),
@@ -400,17 +400,17 @@ export class ActivityTracker {
         : thinkingPhrase(thinkingMs, undefined, isNight(new Date(nowMs).getHours())))
       return {
         phase: this.phase,
-        line: `${phrase} · 总${elapsed}`,
+        line: `${phrase} · total ${elapsed}`,
         phrase,
         toolCount: this.toolCount,
         turnElapsedMs: this.turnElapsedMs(nowMs),
         phaseStartedAt: this.phaseStartedAt,
       }
     }
-    const label = this.phase === 'waiting' ? '等待模型响应' : '思考中'
+    const label = this.phase === 'waiting' ? 'Waiting for model' : 'Thinking'
     return {
       phase: this.phase,
-      line: `${label} · 总${elapsed}`,
+      line: `${label} · total ${elapsed}`,
       label,
       toolCount: this.toolCount,
       turnElapsedMs: this.turnElapsedMs(nowMs),
@@ -421,16 +421,16 @@ export class ActivityTracker {
   private doneSummary(nowMs: number): { line: string; phrase?: string } {
     const { thinkingMs, toolMs, toolCount } = this.stats()
     const tokens = this.turnTokens > 0 ? ` · 🔥 ${fmtTokens(this.turnTokens)}` : ''
-    const base = `${this.donePrefix} · ${toolCount} 工具 · 想${fmtDuration(thinkingMs)} 干${fmtDuration(toolMs)}${tokens}`
+    const base = `${this.donePrefix} · ${toolCount} tools · think ${fmtDuration(thinkingMs)} work ${fmtDuration(toolMs)}${tokens}`
     if (!this.config.phrases) {
-      return { line: `搞定 ✓ · ${toolCount} 工具 · 想${fmtDuration(thinkingMs)} 干${fmtDuration(toolMs)}${tokens}` }
+      return { line: `Done ✓ · ${toolCount} tools · think ${fmtDuration(thinkingMs)} work ${fmtDuration(toolMs)}${tokens}` }
     }
     const last = this.doneQueue.at(-1)
     if (last !== undefined && nowMs - last.endedAt < DONE_FRAGMENT_MS) {
       const fragment = toolFragment(last)
-      return { line: `${this.donePrefix} · ${fragment} · ${toolCount} 工具${tokens}`, phrase: this.donePrefix }
+      return { line: `${this.donePrefix} · ${fragment} · ${toolCount} tools${tokens}`, phrase: this.donePrefix }
     }
-    return { line: base, ...(this.donePrefix === '搞定 ✓' ? {} : { phrase: this.donePrefix }) }
+    return { line: base, ...(this.donePrefix === 'Done ✓' ? {} : { phrase: this.donePrefix }) }
   }
 
   /** The fresh self-narration line, or null once the stream has been quiet. */
